{
  "name": "tnl",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun packages/tnl-app/index.html",
    "dev:canvas-interface-demo": "bun packages/canvas-interface/demo/index.html",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "check:canvas-interface": "tsc -p packages/canvas-interface/tsconfig.json --noEmit",
    "check:tnl-app": "tsc -p packages/tnl-app/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  }
}
